class Pagina {

    inicio: number;
    fin: number;
    delta: number;

    constructor(ini: number, fin: number, del: number) {
        this.inicio = ini;
        this.fin = fin;
        this.delta = del;
    }
}

export default Pagina;